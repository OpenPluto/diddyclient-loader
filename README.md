# diddyclient-loader
it works by using cURL get request to doomsdayclient's website, it downloads the file and calls it lunar.jar then runs it and changes the window title to lunar.exe, when you exit the doomsday java instance, it goes to cmd.exe and clears the page 
